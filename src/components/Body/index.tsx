import { TextField, Button } from "@mui/material";
import React from "react";

const Body: React.FC<any> = (props) => {
  return (
    <>
      <TextField />

      <Button />
    </>
  );
};

export default Body;
